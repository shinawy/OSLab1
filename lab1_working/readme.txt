the submission consists of:
	backup.sh 
	restore.sh 
both are using the functions in the file function.sh 
to run the backup script; provide it with three params which are backupfrom folder and backupto folder and the encryption key 
the restore is working in a reverse way 

the submission also has a design document file and some testing directories 


